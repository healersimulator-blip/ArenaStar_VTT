# Macros / FX Wizard — implementation status

Reference target: [`MACROS_FX_WIZARD_PARITY_SPEC.md`](MACROS_FX_WIZARD_PARITY_SPEC.md). **This is an integrated, incrementally verified implementation, not yet complete Sequencer/Tagger/MATT parity or a redefinition of that target.** The status below is intentionally stricter than “a type/editor field exists”: a criterion remains partial until its entire §10 scenario runs end to end.

## Working now

- **Named GM Revert for nonvisual world effects:** host-authored, bounded private action receipts record exact inverses and post-image hashes for tile traps, PF1e HP/temporary HP changes, chat, Tagger rules, roll applications, prefab placement, summon placement/dismissal and reviewed-script RPCs. A graph's later reviewed scripts/summons share its receipt; multiple successful RPCs form one action even if a later step fails. The GM's **Chat → World actions → Revert** restores an eligible completed action, including its graph history, in one host transaction; this is **not** merely visual FX cleanup or the latest global Undo. Pending workers and subsequently edited documents/dependents are refused, rather than overwriting another person's changes. Receipts survive compaction, reboot and world-file copies; players never receive them. Already-pruned chat entries obey the independent 100-message retention cap and are not resurrected, but do not prevent restoring unchanged mechanical effects. Client-originated edits and unrelated automatic lifecycle events are not retroactively associated with an action; pre-existing external effects cannot be unplayed. See [`e2e/action_revert.spec.ts`](e2e/action_revert.spec.ts) for GM wizard/trap, reviewed Worker and real connected-player click → GM Revert tests.
- **Hurt / Heal (bounded PF1e contract):** a fixed whole HP delta, negative for damage and positive for healing, applies to 1–32 selected linked-token actors with one application per actor. Temporary HP is spent first, healing updates PF1e nonlethal damage, and earlier changes in the same graph are read from staged actor state. Missing/malformed actor HP or a failed later graph step changes nothing. Dice/expression amounts, damage types and non-PF1e adapters remain **partial MATT parity**, not an implied feature ceiling.
- **One wizard window:** chat macros remain usable; GM tabs edit/import media timelines, search/compare up to six owned media files (image/video/audio previews) and send a selection into the current timeline, manage live persistent FX, review independently declared media sharing/export permissions, author active-zone graphs, tags, nested prefabs, summon presets and reviewed JavaScript macros. Players see only published script forms and may use authorized hotbar entries. Graph source/history, script source/grants/approval/return/trace and GM whispers stay private.
- **FX execution and lifecycle:** saved versioned image/alpha-video, text, sound and wait timeline sections; overlapping start times, layers below/above tokens but under fog, transforms/fades, host-resolved destination anchors, image stretching/tint and eased/repeated motion. **One-shot** image/video/text/sound sections can separately replay 2–8 times with a bounded pause between plays (not the same as movement cycles); the host expands at most 64 distinct clock-aligned cues inside the 60-second timeline, and reviewed `playAndWait` waits through the last play. A persistent loop cannot also use section replays. Source/target anchors use the token's **center**, and explicit follow bindings sample only a recipient's currently visible, projected tokens without per-frame socket traffic. GM or published player runs may create **private, host-owned persistent FX instances**; image/text/audio loops restore original host-clock phase after scene changes, reconnect and full reload. The Live FX tab lists/stops individual instances and lets a GM/assistant atomically stop up to 16 current-scene instances by a live name-glob/sequence/token filter in one undoable host transaction; the host rejects over-broad matches without partial stops. `api.fx.play`/`api.fx.stop` work in reviewed scripts, with the invoking player retaining ownership even when code runs under a GM grant. Reviewed `fx` scripts can now list entitled live instances with `api.fx.list({ name?, macroId?, sourceTokenId?, targetTokenId? })` and atomically stop up to 16 matches with `api.fx.stopMatching(filter)`. Names support bounded case-insensitive `*`/`?` wildcards; macro and token IDs match exactly; stop requires at least one explicit selector. A player running GM-elevated code still reads/stops only instances entitled to that actual caller, while an authorized GM can administer the scene. This is not the full Sequencer named effects/sounds API. Host deletion/undo, token/macro/scene deletion, visibility and media-entitlement changes stop/regrant exactly the affected recipients. Completed one-shot runs do not replay on reconnect/reload; replays occur only within the active run. The wizard can place a **point anchor by clicking the map** (Pick on map…, cell/hex-centre snapping, and a cancel that does not touch the draft) and **Preview on canvas** renders the *unsaved* draft through the same `resolveFxSequence` validation and the same renderer **without a host request**: no world op, no `fxInstance`, no recipient, one local pass for a persistent draft, and a Stop button that ends exactly that cue. Media bytes are imported by the GM, never bundled in the repository.
- **Tagger foundation:** typed embedded tags and legacy-flag reads, normalization, exact/case-sensitive default and opt-in any/exact-set, case-insensitive, substring, glob and bounded regex; scene/type/include/exclude queries with stable references, grouped results, a scene-invalidated index, a sidebar with quoted multi-term search, and atomic undoable bulk edits. The GM Tags explorer also has **Apply Tag Rules**: submit 1–32 exact scene-qualified refs, re-read existing `{#}`/`{id}` templates and hidden occupancy on the host, allocate per scene and commit one undoable batch; forged/player requests and any invalid target are rejected without a partial edit. A bounded recent request-ID cache avoids repeat allocation in the same host lifetime (not a durable restart guarantee). Reviewed-script tag reads now support explicit `{ sceneId }` and `{ allScenes: true, groupByScene: true }` across caller-visible scenes (including non-active scenes), bounded to 100 hits/16 KiB; `getTags(ref)` identifies the requested scene and `hasTags(ref, ...)` scopes it automatically. The host strictly validates ref shapes and reprojects from the **actual caller's fresh view on each RPC**; elevated player code cannot inspect hidden placeables or a private scene. GM-reviewed write helpers can now atomically edit up to 32 explicit placeable/scene refs **across caller-visible scenes** with live per-target permissions and undo; caller-run writes still require actual ownership, and no implicit all-scene write is allowed. `{#}`/`{id}` expansion is host-allocated scene-wide on prefab placement, with a shared per-instance ordinal and clone-specific graph reference rebinding, including nested Trigger Tile, Activate/Deactivate and targeted-variable IDs/tag filters plus collection selectors (dangling external targets fail placement).
- **Active tiles:** the GM can create a visible/hidden rotated rectangular tile directly in the Zones editor, publish a graph bound to it, and inspect/reset its bounded recent-fire history. Both GM and connected players can click visible tiles on the actual canvas; the request carries **a tile and hit point, never a private graph ID**. The host verifies scene, projection, rotated hit, owned selected token, replay ID and published player-click gate before resolving private graphs. GM-only graph/trace/history remain off player replicas. Other methods: host-observed committed movement enter/exit/stop/create/rotate and explicit GM manual; rotated rectangular swept hit testing; once-per-token, cooldown, chance, paused/max-run gates; triggering/inside/tile/tag selections. The editor lets GMs set a tile's integer trigger Sort when creating or editing it (players cannot change Sort). The editor now exposes **Shuffle Collection**, 1-based **Position in List**, scene-unit **Filter Tokens by Distance** (tile/trigger centers), bounded read-only **Filter by Attributes** (host-resolved direct/linked-actor paths, typed scalar comparisons or array membership), **Filter by Condition** (active effect names/PF1e condition labels and native actor condition names, with has/lacks), and **Filter by Items in Inventory** (trimmed case-insensitive item names, edge wildcards and typed item-*record* counts), plus **Filter by Token Trigger Count** on *each selected token* (live per-graph history, including this fire, unlike the triggering-token/user check) and **Check Data** on the triggering tile's staged typed attribute regardless of the current collection, with optional fail landing; filters can use a following Check Entity Count for branching, and an independent current triggering-token/user count filter includes this fire; named **Redirect Based on Method/Player Type**, lexical **Loop Through Entities** (nested/snapshot selection with `{{index}}`/`{{currentId}}`), a graph **Reset Tile Trigger History** action, **Set Active Tiles Current Collection** (add/remove/replace/clear), **Run All Batch Actions** (explicit tag/visibility/door queue flush), **Trigger Tile** (ID/current/tag targeting, token source, named child landing and optional Stop return), **Activate/Deactivate** (host-published paused gate on ID/current/tag-matched tile graphs, staged for a later Trigger Tile), and movement **Stop Additional Tiles Triggering**, alongside **bounded persistent graph-local tile variables** (assign/add to this graph or all graphs on scene-local ID/current/Tagger tiles, manual GM clear independent of trigger history, staged for same-plan child triggers and next-fire gates), existing run-local variables/value filters, escaped chat, landings/jumps/stop, tag edits, token/tile/pin show/hide, conditional-door state, sequence cues, **real saved-preset summon steps** and **reviewed script steps with declared literal/context-bound inputs**. Loop depth (8), collection size (1024), execution (up to 25,000 steps), world ops (1024), cues (256), and ordered post-commit script/summon actions (16) are explicitly bounded with errors rather than partial commits; nested graphs additionally cap at 8 depths, 32 tiles/tokens per call and 128 graph invocations. Outgoing loop jumps restore selection and context. A scene clone stages reads for parent and manual child graphs; every invoked graph records history once in the **same** preflighted atomic envelope, and cycles/missing landings fail before any world write. GM-only dry-run/trace and a 600-step acyclic graph are verified; cycles get a budget diagnostic and large trace delivery announces truncation. Mechanical graph ops and history commit in one envelope; FX follows success; queued script and summon actions run in authored order after the graph envelope, with separate authorized commits, bounded compendium resolution and GM-only failure diagnostics. A changed script approval hash is rejected **before** the graph transaction, while a post-commit Worker failure reports an explicit GM-only diagnostic without undoing earlier world actions. The ID-bearing graph request is **GM/assistant-only**: players cannot claim movement triggers, bypass the canvas hit with a guessed graph ID, or activate concealed tiles by guessing IDs.
- **World-clock action:** the Active Zones **Game Time** step advances or rewinds the replicated, host-owned clock by a fixed whole-minute amount (±525,600 minutes per step). The planner stages one coalesced settings op across a graph and its child tiles: later Check Value branches read the changed time, undo restores the clock and trigger history together, and a failed/dry-run graph changes neither. The GM authors the amount before publishing a player-click tile; players cannot patch world settings directly. Clock underflow/overflow and noncanonical settings overrides fail closed. Dynamic expressions, time-trigger scheduling and the rest of MATT's mechanics adapters are still open.
- **Approved programmable macros:** async JS runs in a disposable host-tab classic Worker (not in player tabs); GM-reviewed source+policy hash, caller/approved-GM action grants, bounded typed inputs, live visibility/authority and session checks on every RPC, nested calls with recursion/deadline/action budgets, reconnect replay marker, GM audit and player-redacted results. Scripts can query/edit tags with Tagger-style `getTags`/`hasTags`/`getByTag` and atomic bulk set/add/remove/toggle/clear helpers, send chat, play/list/stop matching FX, fire published automations, call other scripts and atomically place GM-reviewed prefab IDs via an explicit grant/allowlist. Reviewed Workers additionally expose `api.fx.playAndWait(id)` and a bounded `api.fx.sequence().play(...).parallel(...).playAndWait(...).wait(...).call(...).thenDo(...).run()` builder with ordered results. They await finite host-clock cue windows, not client decoding, and do not provide a global scheduler or an atomic transaction. `api.tags.applyTagRules(refs)` explicitly expands already-authored `{id}`/`{#}` templates in one undoable edit; scene-wide numbering is GM/assistant-caller-only because a player could infer the occupancy of hidden tags from skipped numbers. A separate pinned `summons` grant exposes `api.summons.place(presetId, x, y, summonerTokenId)` and `api.summons.dismiss(tokenId)`; player-callable GM-elevated code requires an exact approved preset allowlist, while all callers still pass live caster/scene/placement checks. These script actions commit separately and are **not** a transaction across the whole script. A world ZIP copy **or restore** invalidates imported script approval until the receiving GM re-reviews and republishes it. Workers are **not** hostile-code security sandboxes.
- **Summon lifecycle (partial SU-01–SU-08):** GM-indexed world actors and installed actor packs can be searched, toggled, filtered by source pack/PF1e CR and size, sorted, selected as a stable ref without placement, and saved as scene-bound private or player-published summon presets; a cancellable crosshair previews footprint, grid, range and sight through the shared SQ-10 module (shapes, its own fault list, named reuse) and refuses exactly what the host refuses, while the host still repeats authorization and placement checks. No source actor is permanently imported for a compendium placement. Each success atomically creates a **new linked actor+token**, owned by the actual caller rather than the elevated script, with its own bounded lifetime/instance marker; source media stays GM-only unless the owned hash is player-shareable. Dismissal, expiry, deleting either counterpart/scene, undo and reconnect preserve/clean exactly the intended instance. A published tile graph can invoke a private GM-authored preset on a player's authorized click/movement; source resolution after the graph commit is bounded and revision/caster rechecked, with failure disclosed only to the GM. The script worker can await a typed token result and dismiss only its caller's summons. **Not yet PF1e casting/resource/initiative rules, linked entrance/exit FX or an extensible per-system picker.**
- **Secrecy and media rights:** projected snapshots and updates retract/re-materialize hidden tokens, tiles and map pins; referenced media manifests and **each transferred chunk** follow current visibility and sharing consent. Private FX instances and GM-audience sequence macros never enter player snapshots or catch-up ops, and a revoked viewer receives only an opaque `fx.end`. Prefab instance, parent and source-scene flags stay host-only across creates, updates, scene-embedded-array updates, visibility grants and reconnect snapshots. GM-only graphs/code are absent from player replicas. Multiple writes to one object within a graph envelope now compute visibility crossings against the full pre-envelope state and deliver **one final create/delete**, so nested tag edits plus a hide/reveal cannot leave a stale token/tile on a player's replica. A deletion's pre-image is now used to project undo: a GM-only chat message ID is not sent to spectators, and mixed public/private undo envelopes no longer fail atomically on their replicas. The live Chat panel also refreshes on message **deletes**, not just creates/updates. World exports/imports preserve authored documents while imported JS loses authorization. FX uploads default to **GM-only playback and blocked world export**; the GM must separately attest player-serving and archive redistribution permission. Restricted FX blobs cancel ZIP/folder export rather than entering a half-valid archive. An imported world resets **both** FX rights declarations pending re-review. Legacy pre-wizard assets still need a broader rights migration/audit. Premium asset *platform integration* is not redistribution permission; no paid art is added to this repository.

## Try the integrated path

GM toolbar **Macros** → **Active zones**: expand **Create a rotated tile / zone in this scene**, enter its position/size and create it; choose the tile, enable **click** and optionally **Player click (published)**. Set **Trigger sort** (higher first on a tied movement crossing), change the initial selector to **Tokens in tile**, click **Add: forEach**, then use **Add inside loop** for a chat/tag/FX action (chat supports `{{index}}` and `{{currentId}}`). After the closing marker, add **filter → Tile trigger count** with optional maximum, **routeMethod** to a named **landing**, then save and optionally dry-run. For a relay, save a second **manual** graph on another tile, then add **collection → replace** (e.g. `This tile`) and **triggerTile** targeting that tile to the first graph; both graphs' world writes/history commit as a single undoable plan. Use **Add: Run All Batch Actions** after queued tag/visibility/door edits to execute that batch in order before subsequent steps; later changes to the same document form another batch, while the entire graph still commits or fails as one envelope. A child graph can start at a named landing. For an initially paused child, add **Set tile graph activity → Activate** for its tile immediately before **Trigger Tile**; the child sees the staged gate and the entire change undoes together. Tile visibility does not change, and a graph that deactivates itself completes its current fire. `{{originMethod}}` and `{{originTile}}` report the source event/tile in a child (while `{{method}}` is `manual`). Set the GM canvas layer to **Map** or **GM** to click it; a player clicks it directly in their canvas. Expand a saved zone's **Recent fires** for the last 100 invocations, or **Reset gates/history** to re-arm once/cooldown/max-run rules. **Add: set variable → Tile (persistent)** stores a bounded number/string/boolean value on the private graph (choose Assign or numeric Add; use the separate GM **Clear tile variables** button to erase the map). **Filter → Variable** reads staged changes in the same fire or the value carried over from the previous fire; graph actions and manual GM variable-clear share undo/history. Resetting gates does not erase these tile variables. Hidden tiles may respond to committed movement but are not clickable by players. Graph source, action list and trace never travel with the public tile-click request.

**World-clock branch:** In GM **Macros → Active zones**, add **Check Value → World-clock time of day**, choose `HH:MM` and a comparison/optional failure landing, then append a chat/action after each branch. The host reads the world's replicated settings, interprets 00:00–23:59 as minutes after midnight and wraps at each day boundary; it does not use the browser computer clock. GM **Settings → Advance clock** changes the committed time for the next fire. If the world clock has never been set, it is midnight; malformed stored clocks reject the whole plan. A connected player cannot directly write the clock or supply an event time; a GM-published tile can deliberately let them trigger a pre-authored Game Time change.

**Mechanical trap and script Revert path:** In GM **Macros → Active zones**, select a linked-token actor with PF1e HP, add **Hurt / Heal**, set a negative fixed integer, save a tile graph and fire it manually or publish a tile click to a connected player. The host spends temporary HP before HP; open **Chat → World actions** and press **Revert** on that specific trap to restore HP, temp HP, history and its message. Add a reviewed script step or run a saved reviewed script with world-writing Tagger/chat RPCs: the receipt groups committed writes (a failed later RPC leaves a marked partial action), and one Revert restores them without clearing the at-most-once script execution marker. No Revert during an active worker, and a subsequent actor/tag edit makes the affected action stale rather than silently erasing it.

**Game Time path:** In **Active zones**, place **Game Time → World clock change (minutes)** before **Check Value → World-clock time of day**. A value such as `90` advances the clock from 00:00 to 01:30 in the same graph; a later `-30` rewinds it to 01:00. Save and run or dry-run; Check Value, including in a manually triggered child tile, sees the staged time. Open GM **Settings** to see the replicated clock, and use **Undo** to restore the previous time together with the graph's message/history. Only fixed whole-minute amounts up to ±one year are supported; a negative clock, clock beyond 100 years or shadowing legacy settings document rejects the entire run, not just the clock step.

**Condition/inventory path:** GM **Macros → Active zones**: select **Tokens in tile** or a Tagger token collection, add **condition** (`Prone`, Has or Lacks) and/or **inventory** (`Potion*`, a numeric comparison and matching *item-record* count), then add **filter → Entity count** before a chat/tag/FX action. Names are trimmed and case-insensitive; `*` is allowed only at the start/end of inventory names. The host reads linked actors live even when the actor is GM-only; an unlinked token cannot pass Lacks or zero items. Filtering itself exposes no actor data to players, but the GM's downstream public actions can intentionally disclose the branch result. Other game systems' status registries, stack quantities, expression-based conditions and trigger sources remain open.

**Tile data and per-token history:** In **Active zones**, add **Check Data** to compare a safe path such as `width` on the *triggering tile* even if the current collection is empty; set a failure landing for an alternate branch. Add **Filter by Token Trigger Count** after selecting tokens, set comparison `= 1`, then add **filter → Current collection count** to branch on tokens on their first fire. Each token keeps a separate private history; Reset Tile Trigger History re-arms them and undo restores the previous counts.

**Summoning path:** GM **Macros → Summons** filters indexed world actors or installed packs by name/keyword, pack, size and available CR; **Select reference only** shows its JSON ref without a world import. Choose one, set scene/range/size/duration/LOS, and publish a safe preset (or keeps it GM-only). Use **Pick on map…** to preview/cancel/place. In **Active zones**, add a `summon` step, choose that saved preset and a tile/trigger/current anchor; enable player click if appropriate. The graph history commits once, and the real linked actor/token is independently created after the graph transaction by the host. In **Script macros**, enable the `summons` grant, check the exact preset allowlist for player-callable GM code, review the source+policy, and call `await api.summons.place(presetId, x, y, casterId)`; a second script call can `await api.summons.dismiss(tokenId)`. Typed token inputs are selected from visible scene tokens. The GM retains private source/control and receives post-commit traces; the player receives only a safe callable catalog and their own independent actor/instance. A script's multiple world actions are not atomic; a zone summon cannot roll back graph history if compendium resolution later fails.

**FX path:** GM **Macros → Assets / preview** filters owned image/video/audio files by name, MIME and hash, previews up to six side by side (≤20 MiB per preview), then **Use in timeline** assigns one to a section without copying the media. **FX timelines** authors a multi-lane cue, optionally selects source/target and **Follow visible token anchors**, marks it persistent, and runs it. A point anchor can be placed by clicking the map — **Pick on map…** next to X/Y (and To X/To Y) opens a cancellable crosshair with a cell/hex-centre snap and a live coordinate readout; cancelling, or closing the wizard with this draft, leaves the saved timeline untouched. **Preview on canvas** renders the unsaved draft on the GM's own canvas before anything is saved: it validates exactly like a save and never commits, so no player sees it, no `fxInstance` appears and Undo has nothing to do; **Stop preview** ends the local cue. **Live FX** lists host-owned instances and stops them without touching mechanics; authorized viewers regain an eligible aura after reconnect/reload. For uploaded media, the two separate sharing/export checkboxes default off; **Review existing media permissions** allows GM reapproval after a world import. ZIP/folder export refuses FX bytes lacking explicit redistribution rights. In **Script macros**, a reviewed `fx`-granted script can query `api.fx.list({ name: "ward*" })` and call `api.fx.stopMatching({ name: "Ward Loop" })`; all matching authorized persistent instances stop in one undoable envelope and reappear after undo. Alternatively, **Live FX → Stop matching live FX** accepts a scene, case-insensitive `*`/`?` name and/or exact saved sequence/source/target token IDs; an authorized GM/assistant stops up to 16 matching live instances atomically. The separate list search is only a preview, and a match over 16 is rejected without changes. For an executable chain, save short, nonpersistent timelines and use their saved IDs in a reviewed `fx`-granted script, e.g. `await api.fx.sequence().play("intro").parallel(api => api.fx.play("left"), api => api.fx.play("right")).playAndWait("outro").thenDo(api => api.chat.say("Done", "gm")).run()`. A cue must be scheduled to finish in ≤7 seconds (including host lead time) to be awaited; the Worker has a 10-second deadline. Earlier cues are **not** rolled back if a later section fails.

**Tagger rule path:** GM **Macros → Tags**: select up to 32 scene-qualified results with `{#}`/`{id}` templates already stored (use **Replace** to author them first), then click **Apply Tag Rules**. The host re-reads live hidden tags and allocates per-scene unique values in one undoable commit. The editing text field is not sent as a rule request. Players cannot invoke this GM-only allocation.

**Sequencer replay path:** GM **Macros → FX timelines**: add a text/image/sound one-shot, set **Section play count** to 3 and **Pause between plays ms** to 350, then save and run. This makes three separate clock-aligned plays of the same section; **Motion cycles** instead animates *within* one play. Persistent FX loops cannot also use section replays. A reviewed script may await the final play with `api.fx.sequence().playAndWait(id)`.

**Prefab path:** GM **Macros → Prefabs** captures scene objects and their linked tile graphs, chooses a root and a nested parent/lock for each child, previews bounds/media/reference failures, stamps two or more instances with fresh IDs and scene-unique `{#}`/`{id}` bindings, and lists/despawns entire instances. Token/tile root translation/uniform resize/rotation and root deletion atomically carry/cascade nested children and anchored graphs; undo restores all of them together. Hidden descendants and their media remain absent from player snapshots, reconnects and asset manifests. A GM-reviewed programmable macro may place an allowlisted template using `api.prefabs.place`, including a narrowly published player invocation. The prefab path still lacks **summons as prefab children, a full Sequencer programmable chain and sound/effect filters, non-token/tile root transforms, in-place attach/detach, and the complete MATT action registry**; this path does not satisfy full parity.

### Implemented MATT §5.4 action contracts (incremental)

All are **GM-published graph steps**, resolved on the host from a current event and live scene; players send no step payload. Each name below has a corresponding executable fixture in [`tests/core/automation.test.ts`](tests/core/automation.test.ts); [`tests/host/sync.test.ts`](tests/host/sync.test.ts) covers player movement, atomic update/projection and reconnect; [`e2e/active_zones.spec.ts`](e2e/active_zones.spec.ts) exercises the authoring controls in Chromium. Pure selection/route steps cause no world writes and need no undo entry; persistent writes use the existing atomic undoable graph envelope. Nested calls preserve this rule across parent and children, not as separate commits.

| MATT equivalent | Editor/definition contract | Runtime and authority/undo contract |
| --- | --- | --- |
| Activate/Deactivate | `setActive.mode: activate\|deactivate\|toggle` with scene-local ID/current/Tagger tile targeting | Alters only the private published graph's paused gate, never tile visibility; up to 32 tiles/128 distinct graph edits. Subsequent nested Trigger Tile calls see staged changes; self-deactivation does not cancel the approved current run/post-commit actions. All changes and child history share one preflighted envelope/undo; disconnected/forged players receive no graph IDs. |
| Set Active Tiles Current Collection | `collection.mode: add\|remove\|replace\|clear`, with a scene-local selector except on clear | Stable ref-identity deduplication; add/remove/replace/clear the current graph's targets with staged Tagger matches. Collection changes alone write no world ops. Owner/user collections and inter-graph collection returns remain open. |
| Set Active Tiles Variable | `set.name` with `scope: tile`, `operation: assign\|add`, bounded literal number/string/boolean value and optional `target: id\|current\|tag` (self graph if absent; no expression evaluator yet) | Each graph has a private durable map (≤64 keys; identifier names; bounded values) in its existing state; arithmetic is finite and reads the **persistent** value even if a run-local variable has the same name. Up to 32 same-scene tiles / 128 graph states per plan; each graph bound to a target tile receives its own value, including paused graphs, without incrementing its trigger count. Staged writes feed later child Trigger Tile calls; the current graph's own writes feed filters/chat. All state/history and mechanics share one preflighted undoable envelope. GM **Clear tile variables** clears the map without resetting gates; history reset preserves it. No player can read/edit the map directly. Dynamic handlebar names/values, wildcard updates, delete, scene/global/user variables and other external tiles remain open. |
| Run All Batch Actions | `batchFlush` action in the graph/inside a child or loop; no parameters | Executes the currently queued tag, visibility and door writes **at that graph position**: combines writes to each target into one update per batch, appends the updates before subsequent ops, advances baselines, clears the queue, and starts a new batch for later actions. Child graphs share the queue. The final queue flushes implicitly. Host publication, projection and undo remain **one atomic envelope** even across explicit barriers; runtime/preflight failure discards all batches. Other MATT batch-action families are not yet supported. |
| Trigger Tile | `triggerTile.target: id\|current\|tag`, token source `triggering\|current\|inside`, optional child landing and `propagateStop` | Finds **manual-method, same-scene** GM-authored graphs by target tile on the host. Stage an active-scene copy; parent/child tag and door edits feed later selectors; coalesce repeated histories and per-batch writes into ONE undoable envelope. Depth ≤8, tile/token fanout ≤32 each, graph invocations ≤128, steps ≤25,000, ops ≤1024, cues ≤256, scripts ≤16; cycles, missing target/landing and preflight failures discard the entire world plan. FX/scripts retain their explicit post-commit contracts. No arbitrary cross-scene child ID is accepted from a player. |
| Stop Additional Tiles Triggering | `stopOthers` action | After a successful movement graph commit, suppress later tiles for that same moving token (path fraction, method, descending optional tile `sort`, then IDs). A skipped/failed/dry-run graph does not suppress; a separately clicked tile is the only tile currently dispatched for click. This is not the full MATT overlap/priority algorithm. |
| Shuffle Collection | `shuffle` action on current collection | Host Fisher–Yates RNG; stable dry-run seed; no write/undo. |
| Position in the List | `position.index` (integer, 1-based, 1–10,000) | Keeps that single current target or none; no write/undo. |
| Filter Tokens by Distance | `distance.from: tile\|trigger`, `min?`/`max` in finite scene units | Centers + `scene.grid.distance / size`; rejects missing trigger/non-token targets; no write/undo. |
| Filter by Attributes | `attributes.path` with exact `eq/ne/gt/gte/lt/lte/has` and typed scalar value; `name`/geometry/door etc., own-data `system.*`/`flags.*`, linked-token `actor.system.*`/`actor.flags.*`/name | Filters current token/tile/wall/drawing collection in order; missing/mismatched paths do not match even for `ne`; numeric order only, `has` only for arrays (not template/JS expressions). Host reads staged placeables and live linked actors, not the requesting player's graph payload. No world write; a following count check branches. Path depth ≤8, value ≤256 characters, leaf ≤4096 chars/elements and ≤100,000 comparisons across the nested plan; oversized/unsupported inputs fail without partial commits. GM-only graph and trace are not projected to players. |
| Check Data | `checkData.path` is a tile-only bounded own-data path with typed `eq/ne/gt/gte/lt/lte/has` and optional named failure landing | Compares the **triggering tile**, never the current collection, using the staged scene clone and shared bounded read budget; failure stops or jumps without disclosing private tile attributes to players. Core/host/production Chromium fixtures. |
| Filter by Condition | `condition.effect` (trimmed name, ≤128 chars), `mode: has\|lacks` | Filters only the current token selection: active embedded effect names and `flags.pf1e.condition` labels plus the linked actor's `system.pf1e.conditions` array, matched case-insensitively; disabled effects do not count. Missing/unlinked actors never satisfy either mode; tile/wall targets fail. No actor content is returned in a player replica/trace. Results stay in order, with a separate count step controlling a branch. Actor lists are capped at 4096 records, names at 4096 characters; condition and inventory share a 100,000-read nested-plan budget. Invalid imported data or overflow rejects the whole plan before a world commit. Does not yet integrate other systems' condition registries, durations or named status catalogs. |
| Filter by Items in Inventory | `inventory.item` (trimmed case-insensitive literal, optional `*` only on either edge), `compare: eq\|ne\|gt\|gte\|lt\|lte`, integer `count` in 0–4096 | Reads matching **embedded item documents**, not their quantity/uses; leading/trailing/both `*` give suffix/prefix/contains matching. A missing linked actor cannot match `count = 0` or `!=`. Same token-only, bounded host-read, GM-private and atomic-failure contract as Filter by Condition; further system-specific quantity and inventory modes are not implemented. |
| Check Tile Trigger Count / Check Token Trigger Count | `filter.test: tileCount\|tokenCount` with inclusive `min`/`max?`; optional failure landing | Reads host history **including this fire**; absent triggering token uses a per-caller history key. No separate write; the graph history op is undoable. |
| Filter by Token Trigger Count | `tokenTriggerCount.compare: eq\|ne\|gt\|gte\|lt\|lte`, integer `count` 0–1,000,000 | Filters **each selected scene token** against this graph's staged, GM-private per-token count (including this fire; unknown token = zero). Rejects non-token targets; a later Check Entity Count decides branching. Reset/undo restore per-token histories. Core/host/production Chromium fixtures. |
| Redirect Based on Method / Player Type | `routeMethod.routes` keyed by method or `routeUser.gm/player`, optional fallback, named top-level landings | Host event method/caller role; unmatched cases fall through unless fallback set; no write/undo. |
| Loop Through Entities | Paired `forEach.endId` / `endEach.startId` markers, with insert-inside-loop control | Snapshot selection, one current ref per pass, lexical nesting ≤8, ≤1024 targets, restores collection/context on close or outward jump; body writes share a single atomic graph envelope, while reviewed scripts/FX retain their explicit post-commit policies. |
| Reset Tile Trigger History | `resetHistory` action (also a separate GM button) | Zeroes total/per-token counts and recent history in the graph state op, atomically and undoably, **without erasing persistent tile variables**; gates still apply before the action runs. |
| Check Variable | `checkVariable.name` with optional scene-local `target: id\|current\|tag`, `mode: all\|any\|none`, `compare: eq\|ne\|gt\|gte\|lt\|lte\|mod` and typed value (or integer divisor/remainder); optional named failure landing | Reads this graph or the staged private persistent variables on every graph bound to the selected tiles. An absent value is `null`; comparisons are strict, modulo is finite and integer-only. At most 128 values and 100,000 nested reads per plan; no graph state is exposed to players. Pure branch; the graph's own history and preceding writes still share one undoable envelope. Variable namespaces outside tile-graph state remain open. |
| Check Value | `checkValue.source: darkness\|time\|direction.x\|direction.y`; darkness compares numeric 0–1, world time an integer 0–1439 minutes after midnight, with `eq/ne/gt/gte/lt/lte`; host-observed movement compares `left/right` or `up/down` with `eq/ne`; optional named failure landing | Reads **committed** scene darkness, the replicated world clock (initially committed, then staged after Game Time; daily wrap, unset=midnight), or the host-calculated before/after token movement vector. An invalid stored clock aborts the plan. Missing movement direction fails even for `ne` (manual/click). Nested Trigger Tile propagates the observed origin direction; a player cannot provide or spoof it in a click request or directly edit the GM clock. Pure branch; history stays atomic and GM-private. Keyboard modifiers (unattestable physical presses) and other MATT value sources remain open. |
| Hurt / Heal | `hurtHeal.amount` is a finite nonzero whole HP delta, −100,000…100,000; targets `triggering` or `current` token collection | PF1e linked actors only: ≤32 tokens, deduplicated actors, staged HP/temporary-HP reads across graph steps and an atomic post-image-checked GM Revert receipt together with history/chat. Failed preflight writes nothing, player clicks use only the GM-saved delta. No dice expressions, damage types, arbitrary actor schema adapters or all MATT target families yet. |
| Game Time | `gameTime.minutes` fixed whole-minute delta in −525,600…525,600; no expressions or arbitrary code | Host stages the world-owned clock (default 0 seconds) in action order and coalesces all changes across the root and child graphs into one settings op in the graph's undoable envelope. Later Check Value steps see staged minutes after midnight. Dry-run/failure change nothing; underflow, >3,153,600,000 seconds and shadowing noncanonical settings reject the whole plan. Published player clicks use only the GM-saved delta; a player's direct clock write is denied. Core/host/production Chromium fixtures include multiple changes, child tile, undo, rejection and projection. Formula/dice/handlebar time inputs and automated time-based trigger sources remain open. |
| Random Number | `random.name`, inclusive bounded integer `min`/`max` (up to one million apart) | Uses host RNG (stable seed for dry-run) to set a typed run-local variable for later run-variable filters, escaped chat and later actions; fail-closed invalid RNG. No persistent world write except this graph's normal trigger history and downstream actions. Core fixture covers non-degenerate seeded rolls and subsequent value branching; a production Chromium fixture authors the random step, runs it repeatedly and observes bounded rolls in real chat. |

## Explicit parity gaps (spec IDs)

| Requirement / acceptance | Status | Still required for the full target |
| --- | --- | --- |
| SQ-01–SQ-18; A01–A12, A38–A41 | **Partial** | Bounded host-owned persistent image/text/audio instances, stop/replay, a GM manager and visible-token follow exist. **Camera sections** pan, shake or walk a **path** across each *viewer's own* view (SQ-15): the host resolves and bounds-checks a pan destination — or **every waypoint of a path** — exactly like any other anchor and broadcasts only `toX`/`toY` (a path broadcasts its ordered, host-resolved points), a shake carries no anchor at all, and nothing about a camera cue is a document change — no op, no instance, no recipient, no Undo entry, no new message kind. A camera section cannot loop or replay (≤8 per timeline, ≥100 ms, pan/shake intensity 0.05–1, zoom 0.1–10, path 2–8 waypoints), a pan interpolates the **viewport centre** so a pan-and-zoom cannot drift, a shake decays to zero within a 24 px screen-space budget divided by scale, a path is **2–8 waypoints walked once in order**: the tour starts from each viewer's own current centre, so N waypoints make N legs with the section's time spread evenly across them and its easing applied **per leg**, while the zoom interpolates across the whole path instead of restarting each leg; the end state is explicit (`cameraEnd` — a shake restores the base view, a pan keeps its destination, a path parks on its **last** waypoint), a path whose waypoints all resolve to one point is refused ("a camera path needs two different waypoints"), and the wizard's waypoint editor adds/removes rows and places each waypoint with the same shared crosshair. An image/text section also carries **appearance**: a closed `blend` set (normal/add/multiply/screen/overlay/darken/lighten — an unknown name is refused rather than rendered as `normal`) and **one bounded filter** per section (blur 1–32 px, grayscale 0–1, brightness/saturate 0–2, each kind defaulting to the value the wizard offers), built once at spawn as a real blur or colour matrix and never rebuilt per frame (a filter that **animates** keeps that same instance and is only nudged, D-304), with alpha still composing over it. Clearing either field removes the key rather than sending `undefined`, because the wire is msgpack and would carry it as `null`. A visual can also **animate its own transform**: `scaleTo` grows/shrinks it from its authored scale and `spinDeg` turns it, both eased with the section's own curve (the same shared easing as its motion, the camera pan and the camera path) and applied **per frame from elapsed time** rather than at creation, so a late join starts mid-spin instead of at the still frame. A spin **accumulates** across motion cycles (3 × 120° ends at 360°, no snap back), a stretched image's spin adds to its bearing so a spinning bolt still flies along its own line, and the animation does not rotate a mask (the region lives in the parent's space). A filter's own **strength animates** too: `filterTo` carries it from `filter.strength` to that end on the section's own curve, and with `repeats` it **pulses per cycle** instead of accumulating (a strength is a value, not a bearing — the answer scale already gives); both ends are clamped to the kind's own range (a blur cannot animate to 0: dropping the filter is how an author says that), switching the kind **drops** the animation (16 is a heavy blur and an impossible grayscale), an emptied box removes the key, and a late join starts mid-animation. `inspect` reports the strength read back out of the live filter — a colour matrix stores the author's number in one coefficient — rather than a remembered plan. A visual can also be **masked**: one of the shared crosshair's own shapes (circle/cone/ray/rect — `point` is refused because it has no area) measured in scene units against the scene's grid, resolved by the host into a polygon of offsets from the anchor, so the mask travels with a followed effect and a stored cue replays the same polygon. `invert` cuts the region out instead of confining to it; a broken square/hex grid metric is refused rather than silently read 1:1, and each shape accepts only its own fields. The region itself **animates**: `mask.lengthTo` grows it (traveling as a unit-free ratio of the host-resolved polygon, so a recipient still never learns what "15 ft" is in pixels, and scaling as a whole — a rectangle's width grows with its depth) and `mask.spinDeg` turns it about the anchor, on the section's own easing; the two reuse the transform's own rules exactly (a turn **accumulates** across cycles, a size **restarts**, pinned by one shared cycle helper), a circle takes no turn because it has no facing, an emptied box removes the key, a still region is still drawn exactly once into the *same* graphics an animated one is redrawn into, and `inspect` reports the drawn polygon's `radius`, its own `bounds` and — honestly — `bearingDeg`, which is `null` for a circle and a rectangle because neither polygon says which way it faces. A region can also be **bounded by walls**: `mask.walls` trims the resolved polygon against the scene's own sight segments — the exact list the fog's vision worker is fed, so a window never trims, a closed door does, an open one does not and a locked one does — by taking the **smaller of the two radial extents** at every angle where either polygon bends (including where their edges cross, so a switch between the mask's boundary and a wall's is not chorded), which is why the region stops *at* the wall rather than near it; the trim is computed in the region's own offsets (the polygon keeps travelling with a followed visual) and considers only blockers within the mask's own reach. Because it is baked, a wall-bounded region **cannot animate** — a growth or turn is refused by name and the wizard clears an already-entered one and puts the fields away — and two degenerate anchors are refused rather than drawn: one standing on a wall, and a trim that leaves no area. A **camera section is also targetable**: it carries the sequence vocabulary's own `scene`/`gm`/`caller`, and the host builds the cue **per recipient**, so a viewer outside the audience receives the run *without* that section (a bystander cannot infer a GM-only camera from the socket), while a viewer left with no sections receives no cue at all. **The table also answers for itself**: `fx.media` (0x4e, client → host) is one viewer's word about one asset of a cue it was sent — `ready` (in hand, with what the fetch cost), `late` (in hand, past the section's start), `failed` (`fetch` or `decode`) or `unsupported` (this browser cannot decode the format, said before a byte is fetched or while decoding) — and the host folds those answers into a **second** `fx.delivery` for the same run: counts per asset, the worst asset named by the requester's **own section index** (no asset or user identifier, and a closed reason set instead of a detail string that could carry a URL or a hash), `spoke`/`complete` so the line knows whether it is an answer or a snapshot, and the slowest successful fetch. The answer is **the latest, not the worst**: a fetch that failed at cue start and succeeded when the section needed the bytes has the media, so the later ack replaces it and the client only speaks when its answer changes. The first line goes out at the earlier of a failure (the GM can still stop a cue that is playing wrong), a complete answer, or the run's own media span plus two seconds (never under four, never over a minute, one unref'd sweep timer); exactly **one correction** may follow, and only when the whole answer changed — including a viewer that was silent and has since spoken, because a line still claiming "have not reported yet" after everybody reported would be worse than a late one. A refused format is not even downloaded (`canPlayType` answering `""` is a hard no; a shell without a DOM fetches as usual), a local mute or a zero channel is not reported at all (a device preference stays on the device), a `lateMedia: "skip"` still reports `late` because the bytes *were* there, a draft preview never acks, and a persistent instance or a reviewed-script cue gets no receipt — a loop has no single moment whose answer means anything. Receipts are bounded (32 runs, oldest evicted), expectations are fixed when the cue is fanned out (a session that joined later has no standing to answer), and anything unexpected is ignored rather than answered, so a guessed run id learns nothing. Only a GM/assistant requester reads it, for the same reason a player-initiated request gets no preflight line. Only camera sections take the field; image/text/sound/wait sections refuse it as unknown. The handover is explicit: a real drag/wheel/zoom-button cancels the claim *without* writing a camera (the viewer's gesture wins in the same frame, and the rest of that run's camera track is ignored) while a stop restores the pre-cue view exactly. The wizard's **Pick on map…** now goes through a **shared SQ-10 crosshair** (also used by the summon window): point/circle/cone/ray/rect, cell/hex-centre snap plus 15° direction snapping, an outline the author is shown (and which is checked, so a circle half-buried in a wall is refused), range/LOS/reach constraints drawn from the consumer's own host rules, a live fault list in the host's own order, Esc/cancel that leaves the draft untouched, and committed placements that can be **named and reused** by a later pick (names are panel state — the sequence receives plain points). Both of SQ-12's placement modes now exist: the click above, and **drag source → target** (`Drag source → target…`), where one press-and-drag fills a section's start *and* destination, the drag's own bearing (derived and 15°-snapped, overridable with the rotate buttons) becomes the shape's facing, and its measured length is reported in scene units — a drag checks **both ends** plus the segment between them (`line-blocked`, so a wall across the drag is refused), names the end each fault belongs to ("Start: …" / "End: …"), samples the shape at the source only, places **nothing** if the drag never leaves its starting cell, and leaves the line on screen after the pointer lifts so a name or a nudge can still be added before the one commit button writes it. `summonPlacementError` and the preview now call the same segment rule, pinned by a test that asserts they agree, and the wizard still **previews an unsaved draft locally** — same validation and renderer, but no host request, world op, instance or recipient, and a persistent draft previews one pass. New `fx.list`/`fx.stopMatching` supplies limited wildcard-name and exact origin/token ID matching with owner-scoped, undoable stop, and the GM Live FX manager now offers a separate scene-local, host-revalidated atomic stop by wildcard name or exact sequence/source/target ID (up to 16; stop requests from players are denied). A GM-reviewed Worker now has `fx.playAndWait` and a bounded `fx.sequence()` composition of sequential/parallel host-approved FX, finite waits, callbacks and reviewed nested-macro calls (≤32 sections, ≤8 parallel jobs, 7-second wait budget/finite cue deadline within a 10-second Worker). A production Chromium fixture authors a real saved cue and executes a reviewed sequence chain. **Not** a general Sequencer scheduler: no persistent await, client decode/finish acknowledgement, arbitrary nested sequences, transactional multi-section rollback, cancellation graph or extension API. **Delivery (SQ-13/A10)** is now partly closed: media a timeline will need is prefetched ahead of its cue according to each viewer's own 0–8000 ms window (`0` = lazy fetch at cue time), readiness is measured *before* the fetch (so a slow client is identified instead of hidden), a section more than 120 ms late either starts when its bytes land or is skipped according to that viewer's setting, and the run produces one report line — nothing at all when it arrived on time. A per-device panel (GM Settings and the player's guide) adds reduced camera motion (a pan cuts to its destination, a shake is skipped) and FX-sound muting that never affect anyone else; the OS `prefers-reduced-motion` switch only seeds a default a viewer can override. The host reports a partial audience to a GM requester via `fx.delivery` (`0x4d`, counts only: audience/rights/anchor/media, plus how many recipients received the run **without** a section targeted elsewhere and how many entitled viewers were left with none of it — a withheld section is not a skip), and the wizard warns before a save about registry gaps (asset absent, codec this browser refuses, GM-only media in a scene-audience timeline). **Sound lifecycle (SQ-09/SQ-17)** is now partly closed: a sound section names a **channel** (`sfx`/`music`/`ambience`/`voice`, host-validated as a closed set) and may carry **fades** bounded by its own duration; each device mixes those channels with its own gains (master mute plus four faders), the player re-evaluates gain on a short tick so a fade ramps and a fader moved mid-cue reaches the element, a silenced channel is **skipped without fetching its bytes** (the same `muted` reason as the master switch), a loop fades in once and holds, and the device panel lists what *this browser* is playing with a "Stop here" that leaves the host's instance untouched. Still required: the full chain/programmatic API, persistent lifecycle, the crosshair's shapes-committed-*as*-regions mode (a drag is offered for a visual section's start/destination, not for a camera look-at, a path waypoint or a summon) and the remaining on-canvas player modes, the camera's remaining shapes (targeting is by audience only — no per-viewer names, groups or tokens, a visual/sound section still reaches every recipient, and the delivery notice counts rather than names who was targeted — plus per-leg durations and curved/looping legs), masks/elevation, positional/occluded audio and a dedicated host sound manager, effect masks (blend modes, one bounded filter and host-resolved circle/cone/ray/rect masks with cutouts now exist; polygon-authored masks (a wall bound only *cuts* the four shapes), animation of tint/alpha, keyframes or multi-stop tracks, and filter chains do not; animating a mask's own width/spread independently does not — scale, rotation, filter strength and a region's growth/turn animating a visual do), extensible sections and measured synchronization/performance. One-shot sections now support a host-validated, fixed-gap replay count (2–8), resolved into at most 64 cue windows per nonpersistent run; `playAndWait` computes the final expanded end time. This is not a general cancelable/recoverable Sequencer scheduler. One-shot runs deliberately do not survive reload; an immediate navigation before the asynchronous oplog append is durable still needs an acknowledged save barrier. |
| TG-01–TG-12; A13–A18 | **Partial** | Tag APIs on prototype tokens/regions/system extensions and every object sheet; complete sidebar/autocomplete; rebind references in arbitrary third-party graph/flag schemas (internal saved-tile graph refs are supported), and asset/license-aware import/export workflows. Reviewed-script `getTags`, `hasTags` and `getByTag` can explicitly read caller-visible remote scenes and group bounded all-scene results; `getTags` includes untagged visible objects, while a GM-elevated player still cannot inspect hidden targets or private scenes. Bulk write helpers now accept up to 32 explicit scene/placeable refs across projected scenes, with one undo group and no partial edits if any ref fails; caller-mode writes require per-document ownership, and GM elevation does not disclose hidden targets. Implicit all-scene edits remain open. Reviewed `applyTagRules(refs)` now expands existing `{id}` and GM/assistant-caller-only scene-unique `{#}` templates across up to 32 explicit targets in one undoable envelope. The GM Tags explorer has a separate reference-only `tagger.rules` request/result path: host re-resolves live scene-qualified refs and grants, allocates against hidden/live tags per scene, rejects invalid/duplicate batches, commits a single undoable transaction and returns only to the requesting GM. Deduplication is bounded to recent IDs in the live host; it is not a durable retry protocol. A player may expand `{id}` on visible/authorized refs, but even GM-elevated player code cannot request `{#}` because ordinal gaps would reveal hidden tag occupancy; GM-only rule numbering and player ID rules have host fixtures, and a real reviewed Worker tests apply/undo in Chromium. This restriction also covers a reviewed player-triggered prefab placement with a numbered template, which would otherwise publish a generated tag that skips a hidden scene occupant. Existing read/edit/query and prefab-allocation behavior has unit, host and browser integration coverage. The helpers are a deliberately bounded scene-object subset, not a full Tagger module API. |
| TR-01–TR-17; A19–A27, A40 | **Partial** | Additional pointer/hover/combat/time/region methods, alpha and elevation geometry/LOS, full guards and **every** §5.4 action/filter/flow entry plus extension registry. Recent-fire history can be viewed/reset manually or by a graph action but is not a complete configurable history/debugger. Shuffle, position/distance/attribute/count filters, method/user routing, bounded entity loops, current-collection edits and staged Activate/Deactivate tile-graph gates, **ordered batch execution of the supported tag/visibility/door writes**, and **synchronous, staged, same-scene manual Trigger Tile**, plus an ordered **real summon** post-action, now have unit and browser/host fixtures. `Set Active Tiles Variable` retains private per-graph values across fires/undo/reload and targets same-scene ID/current/Tagger tiles, with staged reads on later child calls. `Check Variable` adds a separate typed All/Any/None predicate over those staged values (this/current/ID/Tagger tiles; strict comparisons, missing=`null`, bounded integer modulo and named fail landing); it is not a general MATT expression/variable evaluator. `Check Value` now reads committed scene darkness, 0–1439 minutes after midnight from the replicated GM world clock (including staged Game Time changes), or host-observed token movement direction; invalid clocks and missing vectors fail closed. Browser fixtures change scene darkness and advance world time, exercising both branch landings and next-day wrap; host/core fixtures cover player clock write denial, clicks, movement, nested calls and secrecy. A fixed-minute **Game Time** step now stages and coalesces world-clock changes across parent and child tile graphs in the same undoable envelope; real Chromium checks the saved wizard flow against the Settings readout and Undo, while host/core tests cover published player clicks, private child graphs, dry-run and invalid clock/failure rollback. A tile-only **Check Data** now reads the staged triggering tile independent of current selection and branches to an optional named failure landing; it is a typed attribute subset, not an arbitrary MATT data expression. **Filter by Token Trigger Count** separately filters every selected token by its live per-graph history (including this fire), followed by an entity count to branch; this is distinct from the existing triggering-token/user count check. GM/editor, player host projection and Chromium fixtures cover pass/fail, undo and invalid targets. Keyboard modifiers, wildcard/dynamic names, delete, scene/global/user variables and expression-based values are still open. The `Filter by Attributes` supports strictly bounded typed own-data paths on placeables and linked actors, **not** arbitrary Foundry datapaths, handlebar expressions or code. Newly added token-only condition and inventory filters evaluate live linked actors privately on the host (active embedded effects/PF1e native names; item records with edge wildcards and numeric record counts), with no false “lacks/zero” match on missing actors and one shared bounded-read budget; other systems' status/inventory semantics, item quantity checks and full expression-based filters remain open. Also open: additional event/direction sources, return-value branches, delay scheduler and richer collection/user/owner sources; extend the batch queue to the other MATT batching families (the implemented flush executes only current supported queued writes, never a mid-graph host commit). Child graphs cannot yet return arbitrary data/collections, run cross-scene or await external scripts. Stop Additional Tiles Triggering currently covers ordered movement candidates, not the full MATT click overlap/priority algorithm. A graph can invoke a reviewed saved script **after** its own commit, with bound inputs, pre-commit hash check and private error trace; it cannot yet await/branch on its return, atomically fold script writes into the graph, or run an inline reviewed code revision. Full Run Macro (including chat/sequence), teleport/movement cancellation, PF1e combat adapters and the other system/mechanics actions beyond this fixed-minute clock subset, plus UI/dialog/journal/environment adapters, remain open. The new summon step correctly preflights caster/preset/range before graph history and rechecks them after async pack lookup; it does not turn all post-commit side effects into a single rollbackable transaction. |
| AS-01–AS-08; A08–A10, A18, A30–A31 | **Partial** | New FX imports independently gate player serving and ZIP/folder export; imported FX permissions require fresh local consent, while legacy blobs remain unaudited. The GM can search, MIME-filter and compare six imported assets, with a bounded 20 MiB local preview; broader creator-pack search and previews are still absent. Still needed: namespaced creator/paid asset registry, license/provenance verification (checkboxes are *attestations*, not legal proof), logical ID and variant selection, dependency resolution, portable rebind UI and a rights migration/audit of pre-wizard assets. A local import/hash/manifest plus authenticated playback is **not** an asset-pack platform or redistribution permission. |
| PF-01–PF-07; A16–A18, A28 | **Partial** | GM-authored nested scene placeables, scene-grid placement, child locks, tag/graph rebinding, atomic parent movement/cascade deletion, child privacy, undo and scripted placement are implemented. Numbered `{#}` templates require an actual GM/assistant placer even when a player invokes an allowlisted reviewed GM script; otherwise scene-unique tag ordinals could reveal hidden tags. Player scripts may still place non-numbered approved templates. Still needed: arbitrary-root transforms, interactive attach/detach and bulk edit, nested FX/summons, third-party serializers, nonportable asset/license rebinding and broader cross-scene import tooling. |
| SU-01–SU-08; A32–A33, A39 | **Partial** | GM source menu searches/toggles world + indexed actor packs with pack/keyword/size/CR filters, numeric CR sorting and GM select-only ref without import, and saves approved scene presets; player catalog, cancellable crosshair, host-owned independent actor/token creation, control, dismiss/expiry/cascades/undo/reconnect, script RPC grant/allowlist, and direct published tile-graph invocation have unit, host and Chromium fixtures. Still required for parity: independent multi-pack toggles and per-system index profiles beyond the current PF1e size/CR display, select-only **macro/API** return and direct allowed UUID/pick API, hex/gridless snap, rotation/entrance/exit FX/ping, bounded actor/token overrides and linked/unlinked policies, PF1e spell/resource/initiative binding, and explicit missing-pack/media preview/compensation. Graph/post-commit script actions are *separate* from atomic graph mechanics, not a false combined transaction. |
| FXMaster-like environment; A29–A30 | **Partial** | Per-viewer FX controls exist (SQ-13/D-295: reduced camera motion, FX-sound mute, preload window, late-media policy) and D-297 adds **sound channels with per-device faders**, host-validated fades, and a device-local "playing on this device" list whose stop silences only this browser — alongside the host-side Live FX instance list and its atomic matching stop. Still **not implemented** for parity: persistent scene/region weather, masks, roof-aware filters and particles. |
| MC-01–MC-11; A34–A37, A39 | **Partial** | Reviewed `api.prefabs.place` and `api.summons.place/dismiss` with exact player-callable GM-approved ID allowlists exist; inline tile Run Code and return-based graph branching, startup/item/cast hooks, positional/context arguments and composite macros, named/everyone/except-caller execution, module API/TS helpers, durable long-term dedup beyond the bounded recent marker, compensation for partial multi-action scripts and extension-registration review. |

## Verification and next dependency order

- On this branch: TypeScript + Svelte check, full ESLint and production Vite build pass; **full** Vitest suite: **3,747 passed**, 12 skipped (295 passing files, 2 skipped), of which one covers the wall bound (in `tests/core/fx.test.ts`: a wall 200 px away cuts a 300 px circle flat at the wall while the open side keeps its reach, the polygon stays in offsets, a mask with no wall in reach is the unchanged 16-gon, the sight rule matches the fog's for window/closed/open/locked/opaque, a growth is refused with its reason, a room caps the region on every side, and an anchor *on* a wall is refused by name), three cover the drag gesture (in `tests/core/crosshair.test.ts`: a line placement carries both ends, its derived-and-snapped direction — an explicit angle still winning — and its length while a click placement has neither field; both ends are checked with each fault naming its end in source-then-target order; the line's own wall is refused as `line-blocked` beside the caster's separate `behind-wall` claim and is not checked without an LoS requirement, and a refused drag commits nothing), six cover the region's animation (two in the D-301 mask block of `tests/core/fx.test.ts`: a growth is bounded in the same scene units as the region while a turn is bounded like the visual's own spin and a circle's turn is refused by name, and resolution turns an authored growth into the ratio 4 while a cone's turn travels as degrees and a still region carries no recipe; and four in `tests/canvas/fxStyle.test.ts`: `fxMaskTransform` follows the two rules including the pulse and the accumulating turn, a drawn region is the host's polygon scaled and turned about its anchor with the readback reporting a cone's axis and a circle's `null`, a growing region is redrawn per frame on the same graphics while a still one's polygon is untouched from spawn to end, and a late join starts mid-animation), eight cover the filter work (four in the D-304 block of `tests/core/fx.test.ts`: the animation's ends are clamped to their **kind's** own range and an orphan `filterTo` is named as itself, the plan carries `to` only when asked for, the strength walks/eases/pulses per cycle and answers the authored start to a NaN age or a zero-length section, and resolution preserves it; and four in `tests/canvas/fxStyle.test.ts`: all four kinds round-trip through `fxFilterReadback` **twice-set** to prove nothing compounds, a deepening blur is one filter instance nudged per frame, a colour animation stays absolute while a still filter never moves, and a late join starts mid-animation), twenty-eight cover the SQ-09 sound work (13 in `tests/core/fxSound.test.ts`: channels and fail-safe defaults, total mix normalization, the fade curve including overlap and the loop rule, gain composition, the audible-at-all helper, the summary line; 6 in `tests/client/fxSounds.test.ts`: registration/teardown idempotence, identity on a reused ID, subscriber emissions, the 64-row bound, a throwing stop contained, stop by run/channel/all; three host-validation cases for channels and fades; and six player-flow cases: author volume × device fader on the real element, a zeroed channel skipping without a fetch, a measured fade-in ramp, a mid-cue fader move, a loop listed and stopped locally, and a host stop clearing the list) and thirty-one cover the shared SQ-10 crosshair — `tests/core/crosshair.test.ts` (22: metrics/snapping, every shape's outline and its malformed-extent refusal, endpoint-touch and parallel walls, the door/window axes, an explicit crosshair-vs-host agreement test, fault order, footprint inset, unit-measured range, an area refused at its outline, commit and naming) and `tests/ui/crosshairPicker.test.ts` (9: request resolution, the summon rules incl. the no-caster exemption, shape switching, named reuse) — The acknowledgment reports the *viewers'* own answers but does not distinguish "muted", "skipped by preference" or "disconnected" from "still fetching" (all are "have not reported yet"), carries no per-recipient expectation map (the asset list is the same for every viewer while only camera sections are targetable), and covers neither persistent instances nor reviewed-script cues; and thirty more cases cover FX delivery (a preload plan, late-media policy and report lines; a device-local preference parser; codec fitness and authoring warnings; the player honouring all of it against a controlled fetcher; and two host cases for the counts-only audience aggregate) and twenty-seven cover the FX camera — `tests/canvas/fxCamera.test.ts` (21: centre interpolation through a zoom, eased midpoint, bounded decaying shake, `null` release on a non-finite elapsed time, and seven for paths — progress 0 parks on the viewer's base view, progress 1 lands on the last waypoint, a three-waypoint tour walks its legs in order without backtracking, per-leg easing, whole-path zoom with a single waypoint degrading to a pan, empty/short/NaN fallbacks, and `cameraAt`/`cameraEnd` agreement), `tests/client/fxViewClaim.test.ts` (4: a drag is not undone by the next frame, a stop restores the base exactly, finished shake/pan endings, a stale cue cannot claim a new scene's view) and six added to `tests/core/fx.test.ts` (two for the pan and four for paths: a bad zoom/easing or a forbidden `to`/`intensity` refused, accepted-section invariants without a repeat, every waypoint host-resolved with an out-of-scene refusal, and all-equal waypoints refused at resolution) — and eleven cover FX appearance: five in `tests/canvas/fxStyle.test.ts` (a real `BlurFilter` whose strength matches, the colour kinds as a `ColorMatrixFilter` with balanced grayscale weights, a spawned visual carrying its blend and its one filter with the live view inspected rather than only the plan, an unstyled section as `normal` with no filter array, per-run read-only inspection, and alpha still composing across a tick) and six added to `tests/core/fx.test.ts` (all seven blends accepted with an unknown one refused by name, filter kind and strength including an unknown inner key, appearance refused on sound/wait/camera, the style plan's defaults and clamping, resolution keeping the style, and the wizard's ranges agreeing with the validator's) — and three cover targeted-delivery feedback — two in `tests/core/fxDelivery.test.ts` (an omitted or zero targeting field still yields no notice, targeted-only and empty-only runs each produce their own sentence, and a mixed run keeps the skip total separate from entitled viewers) and one host case in `tests/host/sync.test.ts` (a two-section timeline whose GM-only camera leaves `skipped` at zero while reporting two reduced payloads, then an all-targeted timeline reporting one recipient and two emptied viewers, with no identity in the payload) — and six cover the animated transform — two in `tests/core/fx.test.ts` (both fields and their bounds with the "no destination needed" case, and both refused on sound/wait/camera sections) and four in `tests/canvas/fxStyle.test.ts` (scale walking the eased path and clamping at both ends with degenerate and NaN inputs, a spin **accumulating** across three cycles with the base rotation riding along, the drawn sprite sampled at 0/500/999 ms and then removed with a stretched bolt adding its spin to its bearing, and a mid-section spawn at the correct phase) — and ten cover effect masks — five in `tests/core/fx.test.ts` (the four shapes accepted with `point`/unknown refused by name, each kind's own field set enforced, metric and spread bounds with the invert boolean, the field refused on sound/wait/camera, and resolution into a 300 px offset ring with the authored units absent from the payload plus a broken metric refused and a gridless scene read 1:1), one in `tests/core/fxInstances.test.ts` (a stored cue's resolved polygon accepted and refused for too few/non-finite/too many points or the authored form) and four in `tests/canvas/fxStyle.test.ts` (the ring filled for a mask and rect-plus-hole for a cutout, a spawned sprite clipped with the mask on its anchor and reported by inspection, the mask moving with a followed anchor, and the mask destroyed with its cue) — and five cover camera targeting: three in `tests/core/fx.test.ts` (the three audiences accepted on a camera and a fourth refused by name with the field unknown on image/sound, one run yielding two payloads with the excluded destination absent from the player's cue, and `scene`/`caller` resolution including a targeted shake) and two host cases in `tests/host/sync.test.ts` (a four-section timeline whose GM run gives each player only the scene pan and whose player run follows the *caller*-targeted pan for that player only, the two viewers' cues sharing one `runId`; and a timeline whose only section is GM-only reaching the GM and nobody else) — alongside the nine crosshair-picker cases in `tests/ui/crosshairPicker.test.ts`. Svelte check reports one pre-existing advisory in `ReplayPanel.svelte`; no blocking issues. `git diff --check` passes. A full-parallel run of the same suite once tripped `pf1eMassBattleScale`'s 250 ms p95 ceiling (273 ms) and is green on re-run — the load-sensitive gate D-291/D-292 recorded, not a code change.
- **Real Chromium production `file://` e2e passes:** the FX timeline suite **26/26**, whose newest two specs run a GM and a joined player over a real socket — one where both viewers hold a shared PNG and the requester reads "media in hand … 1 asset(s) × 2 viewer(s)" while the viewer hears nothing (no news is the right amount), and one where the *player's* browser has `canPlayType` taken away by an init script, so the early line says "2 of 2 viewer(s) … 1 cannot decode this format; 1 have not reported yet" and is then **corrected** to "1 of 2 viewer(s) … (corrected)" as the other viewer's honest answer lands; whose earlier spec, whose newest spec places a real wall through the canvas rail, anchors a 300 px mask 200 px west of it, enters a growth *before* checking "Stop at walls" (asserting the wizard clears it and says why), saves and reopens (both facts survive the host), runs it and reads the **drawn** polygon — the east side stops within 2 px of the wall's own line, the west side keeps −300 px, the north/south keep their reach and the polygon has more than 16 points; whose next spec drives a real pointer drag — asserting nothing is placed before the press, that a press which has not moved is still not a line, that releasing keeps the line (measured "61.0 ft at 30°" from the drawn geometry) and that committing writes `250, 250 → 1250, 950` into the draft and survives a save-and-reopen — whose next spec authors a circle mask of 2 units reaching 8 and samples the drawn polygon (the reach ratio lands on 4, the middle frame strictly between the ends, the last frame past 3.5× — it arrived rather than stopping short), then switches the shape to a cone (asserting the growth resets and that a cone *has* a turn field), saves, runs, and watches the drawn bearing sweep from <3° to >87° with most frames between 10° and 80° (a sweep, not a jump) while the reach holds within a pixel; whose next spec authors a blur moving 2 px → 16 px over 2.4 s, watches the wizard's own claim change with it ("applied once" becomes "Moves from 2px to 16px"), samples the **drawn** strength every frame (40+ frames, a range of 2…16, the middle strictly between 6 and 14, barely moved a few frames in, above 15 at the end), and then clears the box, saves and re-opens the macro to prove an emptied animation field is absent rather than a stored number; whose next spec runs two real browser contexts and reads the GM's notice for a text-plus-GM-only-pan timeline as "reached 2 viewer(s) … 1 saw it without its targeted sections" and then, for a timeline targeted away entirely, as "reached 1 viewer(s) … 1 left with none of it" with the player's own notice log empty; whose next spec authors an image growing 1→2.5 and spinning 360° with `easeInOut`, reopens it to prove both fields survived the host, then samples the **drawn** transform every frame: a range covering 1…2.5 with the middle frame strictly between (it travelled rather than teleported), a few frames in barely moved (a linear ramp would fail that), and the rotation ending above 340° without exceeding 365°; whose next spec authors a circle mask (asserting the panel's bounds are the validator's and that switching kinds rebuilds the shape), saves, reopens, and reads the live sprite's mask back as `{points: 16, invert: false}` — the crosshair's own circle resolution — then turns the same geometry into a cutout (`{invert: true}`) and finally removes it (`mask: null`); whose next spec runs **two real browser contexts**: one timeline with a text cue and a `gm`-audience pan, reopened by the wizard as "GMs only" (the audience is a document fact), then run by the GM — the GM's view parks on the destination while the joined player's camera is *exactly* where it was (never told to move, not moved-and-restored), and flipping that pan to everyone, saving and running again does move the player's view to the same destination; whose next spec authors an image cue with a blend and a filter, checks that the wizard's own bounds are the host's (blur fills 8 and caps at 32, grayscale fills 1), saves and reopens it (`screen`/`grayscale`/`0.5` intact), reads the **live sprite** back as `{ blend: "screen", filter: "grayscale:0.5" }`, then clears both, waits for the host's `seq` before reopening (the clearing bug above is what this step caught) and proves the re-read sprite is `{ blend: "normal", filter: null }`, whose next spec authors a three-waypoint camera path — the middle waypoint placed by clicking the shared crosshair at world (1050, 650), saved and reopened as `mode: path` with all three pairs intact — then samples the live camera every frame during the Run: within 30 px of the first waypoint, within 60 px of the middle one, parked **on** the last one while still more than 200 px away at the halfway sample, and with reduced motion a second Run cuts straight to that last waypoint and stays there (one observed load flake is recorded rather than hidden: the D-297 sound spec's *first* assertion, the device-list row appearing after a fetch and a decode, timed out at 5 s once in a batch run, passed standalone and on re-run, and now has a 15 s ceiling), whose next spec imports a real WAV, authors a music-channel sound with 500 ms fades, proves the channel and fades survive a save and reopen, then watches the settings window's device list show the sound's channel and a gain climbing to the author's own 80 % (the fade having completed), drop to 40 % when the device fader is moved mid-cue, disappear with a "this device" note on **Stop here** (host `seq` unmoved), and finally — with the music fader at 0 — produce exactly one "muted on this device" notice and no list row, whose rewritten anchor spec now proves the shared crosshair end to end (a named placement is listed, reused for the next anchor at the same coordinates, and a destination pick draws a ray's area while still writing only the point) — including the two delivery specs (a viewer's own settings mute a sound so only its text cue renders, with one "muted on this device" notice, then cut a 3-second camera pan to its destination with one "cut by reduced motion" notice; and a two-context run where a GM-audience cue reports "reached 1 viewer(s) — … outside its audience" to the requesting GM while the joined player sees nothing) — including the three new camera specs (the third runs two real browser contexts: one host cue moves both clients' own cameras with `seq` unmoved, the joined player's own middle-drag takes *their* view back by more than the drag distance, and the GM's sweep still completes on the destination while the player's view stays put) (a 600 ms pan parks the viewport centre on the host-resolved destination with host `seq`/`drainOps` unmoved; a middle-button drag mid-sweep moves the view against the sweep and the view is still there 2.4 s later, past the timeline's end; a 1.2-second intensity-0.8 shake is sampled frame-by-frame in-page (peak > 1 px) and returns the base camera exactly, scale included; a wheel takes a running sweep away and keeps its zoom) — as well as the specs for map picking (a click at world `(274, 231)` lands on the cell centre `(250, 250)`, Esc changes nothing) and for the local draft preview (one cue rendered from an unsaved 4-second section, host `seq` unmoved, Stop ends it, and the same sections saved and Run then do go through the host) — against the rebuilt production file. The camera work's shared-path changes (a per-frame sink on the stage ticker and an interaction callback on pan/wheel) were regression-checked separately: `canvas_rail` + `canvas_toolbar` + `join` are **18/18** in 1.3 m on two workers, including the zoom-action and left-drag pan specs; re-checked for the camera path as `canvas_rail` + `canvas_toolbar` + `vision` + `walls` at **19/19** with one worker against the rebuilt production file, and the `summons` suite alongside `fx_sequence` is **30/30** (one recorded SQ-09 sound-spec batch flake, 5 s load, passed standalone and on the batch re-run); the canvas/interaction batch (`canvas_rail` + `canvas_toolbar` + `vision` + `walls` + `join`) is **20/20** after hardening its fog-mask spec, which failed one run in three by reloading before the IDB oplog append finished, with the existing `drainOps()` durability barrier. It holds **36 tests** together with the Active Zones, reviewed-script, prefab, summon and new [`action_revert.spec.ts`](e2e/action_revert.spec.ts) suites (connected-player tile damage → GM Revert, and reviewed-script world-write → GM Revert); run as one two-worker batch, two `active_zones` specs hit their 30 s timeout under load (one awaiting a click, one still booting) and `active_zones.spec.ts` alone with one worker is 16/16, the load flake D-291 named. The rest of the Chromium suite and Firefox/WebKit were not rerun in this check. The wizard tests author live Check Data pass/fail on the triggering tile with an empty token selection, per-token first-fire/second-fire branching and undo, a fixed-minute Game Time graph with staged Check Value branches, Settings readout and undo, host-allocated cross-scene Tagger rules with undo and reload, and three timed Sequencer section replays observed on the real Pixi layer with a reviewed Worker waiting until the last cue; they also author a host-RNG Random Number chat flow plus darkness and replicated-world-clock Check Value branches (including Settings time advance and next-day wrap) and a persistent Check Variable check with staged writes and undo/reload, plus a numeric attribute filter followed by a count branch, a condition/PF1e native-condition plus inventory-record-count filter using a real linked actor (and edit its threshold to reverse the branch), a paused child tile that receives a staged remote variable before a same-transaction Trigger Tile call (including undo of variable/gate/history/chat), a persistent tile counter across fires and reload, a two-token loop, a two-tile relay with current-collection edit/atomic Trigger Tile call, an ordered batch barrier with editable tile Sort, a private-preset summon from a saved tile, and a connected player invoking GM-reviewed code to summon and dismiss a private-source instance; host fixtures check private child graphs, coalesced parent/child Game Time steps with staged clock checks and rollback, combined history/undo, batch queue execution/failure rollback, stage-aware Tagger selectors, GM-only message/trace projection, repeated child gates, sorted movement suppression with player priority forgery rejected, visibility/asset grants/revocations and reconnect. FX coverage exercises a real reviewed Worker running a bounded saved-sequence chain with parallel FX, an awaited host-clock cue and a downstream callback; a separate Worker lists and atomically stops two persistent named FX with an undo/playback restoration, a GM Live FX wildcard bulk stop with immediate playback/undo restoration, caller-projected remote-scene Tagger reads and an undoable explicit remote-scene edit, plus reviewed-Worker Tagger rule expansion/undo on a live token, six-way-browser entry, compare/use-in-timeline, separate media sharing/export guard and reapproval, visible-token follow on a real move and reload, and host stop via Live FX. Host fixtures also check private linked actors' live attribute, active condition and embedded inventory values without projecting the graph/actor/GM chat (including a connected player click, undo and stale-seq reconnect), bounded attribute and cumulative condition/inventory reads with no partial writes, remote tile variable fanout into private and paused graphs, atomic rollback on overflow, and clone-specific prefab ID/Tagger rebinding for variable and activation targets. The summon picker browser fixtures verify world-source toggling, actual installed PF1e Bestiary pack filtering by size/CR/keyword, a no-import select-only reference, cancelled crosshair, pack and world placement, persistence and dismissal, plus the SQ-10/A04 pair of circle-and-ray checks on a real wall: the joined player's crosshair refuses the cell beside that wall for a circle (rim crosses it) and for a ray aimed at it (far end crosses), a click changes nothing, the same circle on open floor commits exactly once, cancelling leaves one instance and the same `seq`, and a hand-typed point behind the wall is refused by the host with the player-facing reason; host fixtures cover private pack-source revocation during async lookup, caster/range preflight, post-await policy rechecks and recipient-safe reconnect. The reload/stop browser fixture waits for the asynchronous IDB oplog append before navigating away; a user who reloads in the instant before that append finishes is not yet promised durable acknowledgement. These are **not** the full 41-scenario or cross-browser acceptance matrix.
- Continue in §11 order: complete Tagger/prefab reference rules and asset rights; extend shared sequence scheduler/effect manager and browser acceptance; complete MATT registry/geometry/triggers and GM-reviewed graph code adapters; join PF1e, summons, environments and attachments; run **all 41 acceptance scenarios** plus per-action fixtures and measured browser profiles. Keep each unavailable row visible here until it actually passes its criterion.
