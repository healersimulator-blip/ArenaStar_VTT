/** src/client barrel (§18): Client Core — ClientSync (§5) and later AssetCache. */
export {
  ClientSync,
  DEFAULT_OPTIMISTIC_POLICY,
  type ClientEvents,
  type ClientSyncOptions,
  type OptimisticPolicy,
} from "./sync";
export * from "./assets";
